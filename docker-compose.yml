version: '3'
build:
services:
  nextjs:
    build:
      context: .
      network: host
    # networks:
    # - nextts-rest-redux-panel-nginx_host
  nginx:
    build:
      context: ./nginx
      network: host
    # networks:
    # - nextts-rest-redux-panel-nginx_host
    ports:
      - 80:80
# networks:
#   host:
#     driver: host
# networks:
#   default:
#     external:
#       name: host
